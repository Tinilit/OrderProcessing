# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: orderprocessing
metadata:
  template: dotnet-aspire@1.0.0

services:
  app:
    project: ./OrderProcessing.AppHost
    language: dotnet
    host: containerapp
